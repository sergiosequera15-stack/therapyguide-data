# Microbiología DATA — Worklog 2026-06-22

## Regla operativa

Trabajo remoto únicamente: ChatGPT + GitHub. No asumir PC local del usuario.

## Objetivo de esta tanda

Preparar DATA para una futura consulta APP segura por centro/ámbito, sin mezclar datos HUVN globales con centros específicos y sin activar soporte clínico.

## Cambios realizados

### 1. Catálogo de ámbitos/centros

Nuevo archivo:

- `docs/microbiology/scope_catalog.json`

Define:

- `huvn` — disponible como extracción estructurada pendiente de revisión.
- `hospital_general` — no publicado.
- `neurotrauma` — no publicado.
- `materno_infantil` — no publicado.
- `pediatria` — no publicado; se aclara que pediatría aparece en el índice como población pediátrica HUVN, no como `scope` independiente.
- `extrahospitalario` — no publicado.
- `service_specific` — no publicado.

Reglas clave:

- Centro/ámbito obligatorio.
- Microorganismo/mecanismo opcional.
- Antibiótico/sigla opcional.
- Si los filtros opcionales están vacíos, la APP puede devolver todos los registros del scope seleccionado, con límite visual.
- Prohibido usar HUVN global como si fueran datos específicos de Pediatría, Neurotrauma, Hospital General o Materno-Infantil.

### 2. Estado de explotación del PDF

Nuevo archivo:

- `docs/microbiology/extraction_status_2025.json`

Declara explícitamente que el mapa completo no está explotado ni revisado:

- Catálogo de secciones desde índice: 66.
- `microbiology_map_2025.records`: 0.
- Registros de mecanismos de resistencia: 43.
- Datasets estructurados de sensibilidad publicados: 4.
- Enterobacterias no consolidadas de forma validada.
- QA de enterobacterias no publicado como dataset de consulta.
- Revisión manual no completada.

### 3. Índices actualizados

Actualizados:

- `docs/data_modules.json`
- `docs/microbiology/microbiology_query_manifest.json`
- `docs/manifest.json`
- `tools/update_data_module_manifest.py`

Nuevas URLs expuestas:

- `scopeCatalogUrl: microbiology/scope_catalog.json`
- `extractionStatusUrl: microbiology/extraction_status_2025.json`

### 4. Validación remota reforzada

Actualizados/creados:

- `tools/validate_data_modules.py`
- `tools/validate_data_module_index.py`
- `.github/workflows/sync.yml`
- `.github/workflows/validate.yml`

El workflow ahora valida también el índice que consume la APP.

### 5. Estado de validación

El usuario informó que la validación remota quedó verde después de estos cambios.

No convertir este hito en autorización clínica: solo significa que la estructura DATA y los validadores pasan.

## Estado real tras esta tanda

DATA está mejor preparado para consulta APP por centro, pero la explotación del PDF sigue incompleta.

No afirmar que existe mapa interactivo completo.

No afirmar que existen datasets específicos de Hospital General, Neurotrauma, Materno-Infantil o Pediatría.

El único ámbito con dataset estructurado consultable por ahora es HUVN global, y sigue pendiente de revisión manual.

## Próximos pasos seguros

1. Volver a APP.
2. En APP, no tocar `DataModuleQueryManager.tsx` entero desde un conector que no permita leerlo correctamente.
3. Implementar la consulta por centro solo cuando se pueda hacer un cambio remoto verificable y validado.
4. Mantener la consulta limitada a datos extraídos pendientes de revisión, sin recomendación ni ranking.
5. No EAS build hasta Preflight APP verde y cambio real justificado.
